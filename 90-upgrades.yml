---

- name: Upgrade all hosts
  hosts: all
  become: true
  become_user: root

  tasks:
    - name: Update and upgrade apt packages
      ansible.builtin.apt:
        update_cache: true
        upgrade: true
