---
# Right now, all "logical" hosts are all on the same "physical" host/VM.
# However, for future extensibility, we already group hosts by logical function.

# Publicly hosted FMD Server
hosted:
  hosts:
    foxtrot.fmd-foss.org:
    lintu-server.duckdns.org:

# Package downloads and releases
packages:
  hosts:
    foxtrot.fmd-foss.org:
