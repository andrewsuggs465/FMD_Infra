---

- name: Nginx reverse proxy setup
  hosts:
    - hosted
    - packages
  become: true
  become_user: root

  tasks:

    - name: Install from apt
      ansible.builtin.apt:
        install_recommends: false
        state: present
        name:
          - nginx
          # certs
          - ca-certificates
          - certbot
          # logs
          - logrotate
          - libnginx-mod-http-geoip
          - geoip-database
          # firewall
          - ufw

    # OCI Ubuntu ships with a blanket iptables REJECT rule that fires before ufw.
    # Insert explicit ACCEPT rules at the top of INPUT so they take priority.
    - name: Allow HTTP and HTTPS in iptables (IPv4)
      ansible.builtin.shell: |
        iptables -C INPUT -p tcp --dport 80 -j ACCEPT 2>/dev/null  || iptables -I INPUT -p tcp --dport 80 -j ACCEPT
        iptables -C INPUT -p tcp --dport 443 -j ACCEPT 2>/dev/null || iptables -I INPUT -p tcp --dport 443 -j ACCEPT
      changed_when: true

    - name: Allow HTTP and HTTPS in iptables (IPv6)
      ansible.builtin.shell: |
        ip6tables -C INPUT -p tcp --dport 80 -j ACCEPT 2>/dev/null  || ip6tables -I INPUT -p tcp --dport 80 -j ACCEPT
        ip6tables -C INPUT -p tcp --dport 443 -j ACCEPT 2>/dev/null || ip6tables -I INPUT -p tcp --dport 443 -j ACCEPT
      changed_when: true

    - name: Install iptables-persistent to survive reboots
      ansible.builtin.apt:
        name: iptables-persistent
        state: present
        install_recommends: false

    - name: Save iptables rules
      ansible.builtin.shell: netfilter-persistent save
      changed_when: true

    - name: Open SSH, HTTP, and HTTPS in ufw
      ansible.builtin.shell: |
        ufw allow 22/tcp comment 'SSH'
        ufw allow 80/tcp comment 'HTTP (ACME challenge)'
        ufw allow 443/tcp comment 'HTTPS'
        ufw --force enable
      register: ufw_out
      changed_when: "'Rules updated' in ufw_out.stdout"

    #
    # access logs
    #

    - name: Disable default access log
      ansible.builtin.lineinfile:
        path: /etc/nginx/nginx.conf
        regexp: '^[\s#]*access_log /var/log/nginx/access.log;'
        line: '# access_log /var/log/nginx/access.log'
      notify: Reload nginx

    # No IP, only country. No User-Agent.
    # Based on https://fdroid.gitlab.io/jekyll-fdroid/en/2019/04/15/privacy-preserving-analytics.html
    - name: Set nginx access log
      ansible.builtin.blockinfile:
        path: /etc/nginx/nginx.conf
        # Must be in the http{} block. But must also not have two http blocks.
        insertafter: '^\s*http\s*\{'
        block: |
          geoip_country /usr/share/GeoIP/GeoIP.dat;
          log_format privacy '0.0.0.0 - $remote_user [$time_local] "$host" "$server_port" "$request" $status $body_bytes_sent "$http_referer" "-" $geoip_country_code';
          access_log /var/log/nginx/access.log privacy;
        prepend_newline: true
        append_newline: true
      notify: Reload nginx

    - name: Set up logrotate
      ansible.builtin.copy:
        dest: /etc/logrotate.d/nginx
        owner: root
        group: root
        mode: "u=rw,g=r,o=r" # these are strictly required by logrotate.
        # Rotate daily, keep the last 60 days.
        # Because as volunteers, we cannot check the logs often, so keep them a bit longer.
        # And logs should not contain private data anyway.
        # https://manpages.ubuntu.com/manpages/jammy/en/man8/logrotate.8.html
        # https://nginx.org/en/docs/control.html
        content: |
          /var/log/nginx/access.log {
              daily
              rotate 60
              missingok
              sharedscripts
              postrotate
                  [ ! -f /var/run/nginx.pid ] || kill -USR1 `cat /var/run/nginx.pid`
              endscript
          }

    #
    # HTTP/80 conf
    #

    - name: Create webroot directory for ACME
      ansible.builtin.file:
        path: /var/www/acme/
        owner: www-data
        group: www-data
        mode: "u=rwx,g=rwx,o=rx"
        state: directory

    - name: Copy ACME conf to sites-available
      ansible.builtin.copy:
        src: "files/nginx/acme.conf"
        dest: "/etc/nginx/sites-available/acme.conf"
        owner: root
        group: root
        mode: "u=rw,g=rw,o=r"
      notify: Reload nginx

    - name: Symlink ACME conf to sites-enabled
      ansible.builtin.file:
        src: "/etc/nginx/sites-available/acme.conf"
        dest: "/etc/nginx/sites-enabled/acme.conf"
        state: link
      notify: Reload nginx

    # HTTPS/443 confs and getting certs are in dedicated playbooks

    - name: Enable certbot renewal timer
      ansible.builtin.systemd_service:
        name: certbot.timer
        enabled: true
        state: started

    - name: Reload nginx after certbot renews
      ansible.builtin.lineinfile:
        path: /etc/letsencrypt/cli.ini
        regexp: '^[\s#]*deploy-hook = systemctl'
        line: 'deploy-hook = systemctl reload-or-restart nginx'

    # - name: Cronjob to renew certificates
    #   ansible.builtin.cron:
    #     name: renew certs
    #     job: 'certbot renew --deploy-hook "systemctl reload nginx"'
    #     user: root
    #     hour: "3"
    #     minute: "45"
    #     state: present

  handlers:
    - name: Reload nginx
      ansible.builtin.systemd_service:
        name: nginx
        enabled: true
        state: reloaded
